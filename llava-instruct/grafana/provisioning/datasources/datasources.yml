apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Grafana runs on the host network (see docker-compose.yml) so it talks
    # to the host-network Prometheus over localhost.
    url: http://localhost:9090
    isDefault: true
    editable: false
