# asset-management 背景知识

本文面向不熟悉多模态大模型（VLM）的读者，用通俗方式解释：

1. VLM 是什么、它的输入输出长什么样
2. "多模态指令数据"到底说的是什么数据
3. 有哪些数据集和 VLM 可以帮助理解这种服务范式

---

## 1. VLM 是什么？输入输出是什么？

**VLM（Vision-Language Model，视觉语言模型）** 是一种能同时理解"图像"和"文字"的大模型。它把两种输入对齐到同一个语义空间，然后根据指令输出文字（或坐标、标签等结构化结果）。

### 1.1 架构：三个部件

```
                    ┌─────────────────────────┐
  文本 prompt ─────►│  大语言模型 (LLM)        │
                    │  e.g. Llama / Qwen      │
  图像 ──► 视觉编码器 ──► 视觉 token ──► 投影层 ──►│  ◄── 拼接
         (CLIP/ViT)                    │       │
                    └─────────────────────────┘
                                   │
                                   ▼
                            输出文本 token
```

- **视觉编码器**：把图片切成 patch 并编码成"视觉 token"（类似文本里的词向量）
- **投影层**：把视觉 token 翻译成 LLM 能读的表示（对齐图像空间和文本空间）
- **LLM**：把"文本 token + 视觉 token"一起做自回归推理，逐词生成回答

### 1.2 输入：两种模态拼在一起

VLM 的输入本质上就是一个**混有图像占位符的文本序列**：

```
用户：<image> 这张图里有什么动物？
      ↑
   这里插入图片的视觉 token
```

在代码/数据层面，图像位置用特殊 token 标记，最常见的是 `<image>`。

### 1.3 输出：取决于训练时的任务

| 输出类型 | 示例 | 对应能力 |
| --- | --- | --- |
| 自然语言 | "一只狗坐在沙发上" | 描述 / 问答 |
| 结构化文本 | "3"、"上升趋势" | 计数、图表阅读 |
| 坐标 | `[124, 56, 300, 210]` | 区域定位（grounding） |
| 标签 | "close_up"、"pan_right" | 分类 / 受控词表 |

> 关键点：**VLM 的输出方式由训练数据的"问答形式"决定**。数据里怎么问、期望怎么答，模型就学成什么样。这就是本项目（指令数据工厂）存在的意义。

---

## 2. "多模态指令数据"是什么？

### 2.1 从指令微调说起

大模型有两条关键训练阶段：

1. **预训练**：在海量图文对上"看懂世界"（学表征，输出是一句话描述）
2. **指令微调（SFT）**：在"问题→答案"对上学**服从指令**（用户怎么问，模型怎么答）

"指令数据"（instruction data）指的就是第二阶段用的数据：**一条条"问题 + 期望回答"**，让模型学会问答这种服务范式。

### 2.2 多模态指令数据 = 图文 + 任务

多模态指令数据在"问题→答案"基础上多了图像，并且**按任务分类**，而不是只有一种"描述这张图"：

| 任务 | 输入 | 期望输出 |
| --- | --- | --- |
| 图像描述 | `<image>` 描述这张图 | 一段场景描述 |
| 计数 VQA | `<image>` 图里有几只狗？ | 3 |
| OCR 摘要 | `<image>` 读出并总结文字 | 文字摘要 |
| 文档问答 | `<image>` 营业收入是多少？ | 从图中读到数值 |
| 图表阅读 | `<image>` 销售额趋势如何？ | 上升/下降及原因 |
| 区域定位 | `<image>` 定位图中的猫 | [x, y, w, h] |
| 多图比较 | `<image><image>` 两图有何不同？ | 差异分析 |

### 2.3 LLaVA 对话数据格式（本项目的产出格式）

LLaVA 用多轮对话格式组织样本（本项目 `conversations` 字段的来源）：

```json
{
  "id": "asset_1_image_description_0",
  "image": "images/cat.jpg",
  "asset_type": "general_image",
  "task_type": "image_description",
  "conversations": [
    {"from": "human", "value": "<image>\nDescribe this image in detail."},
    {"from": "gpt",  "value": "一只橘猫坐在沙发上，背景是客厅。"}
  ]
}
```

- `from: human` 的 `value` 中 `<image>` 标记图片插入位置
- `from: gpt` 的 `value` 是期望答案（训练时的监督信号）
- 训练时框架会把 `<image>` 替换成视觉编码器输出的 token

### 2.4 为什么不能只用 caption 数据训练

原始图文对（caption）只有"一张图配一句话"，导致：

- 只会被动描述，不会主动回答提问
- 没有任务分层：不会定位、不会读图、不会比较
- 风格不统一，对话感差

所以需要像本项目这样的工厂，把 caption/OCR/bbox 等"素材"**重写为任务化的问答对**。

---

## 3. 有哪些公开数据集可以参考？

### 3.1 基础图像 + 标注（资产层来源）

| 数据集 | 内容 | 本项目用途 |
| --- | --- | --- |
| **COCO** | 33 万张自然图像 + 5 种标注（caption/检测框/分割/关键点/全景） | 通用图像资产 + bbox 来源 |
| **TextCaps / OCR 数据** | 带文字的自然图 | OCR 相关任务 |
| **DocVQA** | 文档图像 + 页面级问答 | 文档问答任务设计参考 |
| **ChartQA** | 图表图像 + 数值问答 | 图表任务设计参考 |

### 3.2 指令数据（VLM 训练直接可用的监督数据）

| 数据集 | 规模/形式 | 特点 |
| --- | --- | --- |
| **LLaVA-Instruction-150K / 665K** | 用 GPT-4V 生成的图文问答 | 最经典的 LLaVA 训练数据 |
| **ShareGPT4V** | 120 万图文 | 高质量重描述 + 指令 |
| **LLaVA-v1.5 混合数据** | 665K（VQA/OCR/区域对话/文本） | 社区公认的 SFT 混合配方 |
| **RefCOCO / RefCOCO+ / RefCOCOg** | 指代分割（referring expression） | grounding 数据标准 |
| **Visual Genome** | 场景图（区域+物体+关系） | 区域级问答来源 |

### 3.3 评测数据（衡量"做得好不好"）

- **MMBench / MMMU / MME**：综合多模态能力榜单
- **DocVQA / ChartQA**：文档与图表专项
- **RefCOCO**：grounding 专项
- **TextVQA**：图内文字问答

> 建议上手路径：先看 **COCO**（素材从哪来）→ **LLaVA-665K**（指令数据长什么样）→ **MMBench**（做完怎么验）。

---

## 4. 有哪些 VLM 可以体验这种服务范式？

按发布时间线，从经典到最新：

| 模型 | 发布 | 特点 | 适合做什么 |
| --- | --- | --- | --- |
| **BLIP-2** | 2023.01 | 轻量查询变换器桥接图文 | 理解"对齐"原理 |
| **MiniGPT-4 / InstructBLIP** | 2023.04 | 指令微调风格 | 理解指令数据的作用 |
| **LLaVA / LLaVA-1.5** | 2023.10 | 本项目的核心对象，数据驱动成名 | **必看**：论文 + 训练数据开源 |
| **LLaVA-NeXT** | 2024 | 高分辨率切片 + 视频 | 工程化扩展 |
| **Qwen2-VL / Qwen2.5-VL** | 2024-2025 | 中文强、文档/图表/视频全面 | 实际生产中常用 |
| **InternVL2.5 / InternVL3** | 2024-2025 | 开源视觉 tokenizer、规模大 | 中文场景、教学复现 |

### 体验方式（不需要 GPU）

- **HuggingFace Space**：搜索 "LLaVA" 或 "Qwen2.5-VL" 有在线 Demo，直接传一张图提问
- **API**：Qwen-VL 系列在 DashScope/硅基流动等平台有免费额度
- 本地 CPU 跑不动大模型，但可以先用上面的 Demo 感受"输入图+指令 → 输出回答"的范式

---

## 5. 把背景串起来：本项目在这条知识链里的位置

```
COCO 等原始图像 ──► 多模态资产池（本项目 asset-management 第 1 步）
                        │
                        ▼
           模板化任务派生（描述/OCR/图表/grounding/多图）  ← 你在这里
                        │
                        ▼
          LLaVA 对话格式样本 + QA 质检 + 可视化反查
                        │
                        ▼
        train/val/smoke + manifest（可直接喂给训练框架）
                        │
                        ▼
        LLaVA / Qwen2.5-VL 等模型进行指令微调（SFT）
                        │
                        ▼
        MMBench / DocVQA / ChartQA 评测验证效果
```

**一句话总结**：VLM = 视觉编码器 + 大语言模型 + 投影层，输入"图+指令"输出"回答/坐标/标签"；多模态指令数据 = 把图像素材组织成按任务分类的"问-答"监督对；项目 `asset-management` 就是批量生产这类数据的工厂，产物可直接训练 LLaVA/Qwen2.5-VL 这类模型。

---

## 6. 延伸阅读

- LLaVA 论文：Liu et al., "Visual Instruction Tuning" (NeurIPS 2023)
- LLaVA-1.5 论文："Improved Baselines with Visual Instruction Tuning" (CVPR 2024)
- LLaVA 官方仓库：github.com/haotian-liu/LLaVA（含数据/训练/推理全套）
- HuggingFace：huggingface.co/datasets/liuhaotian（官方指令数据集）
- Qwen2.5-VL：huggingface.co/Qwen/Qwen2.5-VL-7B-Instruct
